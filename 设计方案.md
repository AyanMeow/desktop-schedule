# 桌面日程管理软件 — 第一版设计方案

> 文档版本：v0.1（初稿）
> 日期：2026-07-26
> 平台：Windows 10 / 11
> 性质：单人开发的轻量级、常驻桌面日程小组件

---

## 0. 文档目的

本文档基于对现有同类软件与技术栈的调研，给出本项目（暂定名 **"桌面日程"**）的第一版设计方案，覆盖：产品定位、需求映射、技术选型、架构、数据模型、交互与视觉、里程碑。它是后续编码实现的纲领文件。

调研结论摘录（详见同目录 `调研报告.md`）：

- **竞品空白**：现有产品中，"桌面壁纸层嵌入 + 透明 + 完整待办逻辑 + ddl 动态着色 + 完成鼓励语" 这一组合**没有直接对标成品**。
- **可借鉴点**：小黄条/DesktopCal 的壁纸嵌入（R1/R2/R4）、TickTick 的着色与 Countdown（R7）、敬业签的自动划线 + Forest 的自定义鼓励语（R5）、Todoist 的自然语言范围填充（R6）。
- **技术选型**：综合轻量、透明无边框托盘覆盖度、单人开发效率，**Tauri 2.x 为首选，WPF/.NET 8 为兜底**。

---

## 1. 产品定位

一句话：**它是桌面的一部分，而非一个应用。**

- 形态：常驻桌面的半透明日程小组件，像一张贴在壁纸上、可随手勾选的"日历便签"。
- 不做：账号体系、云同步、团队协作、复杂项目管理（这些都是 TickTick/Notion 的赛道）。
- 做好：低打扰、低占用、低录入摩擦、即时反馈。

目标用户：需要在桌面常看到一个清爽日程、并随手完成/记录的 Windows 用户。

---

## 2. 需求映射与功能规格

将原始 8 条需求逐条细化为可实现的功能规格（带验收标准）。

### R1. 无边框显示在桌面，自由拖拽，可锁定位置；最小化到托盘不影响显示

**规格：**
- 无边框半透明窗口，**采用普通窗口（非置顶、非壁纸层嵌入）**——即正常 z-order，不抢置顶、可被其他窗口遮挡，需要时点一下即恢复可见。这是三档窗口形态里最简单、最稳的方案，规避了 Win32 壁纸层 hack 和置顶抢焦点的风险。
- 拖拽：按住窗口非交互区域（标题条/空白处）拖动；锁定开关开启后，窗口位置固定且忽略拖拽事件。
- **托盘与桌面贴片解耦**：这是关键设计——"最小化到托盘"只隐藏【设置/详情等次级窗口】与【任务栏图标】，**主桌面贴片始终保持可见**。托盘菜单提供"显示/隐藏贴片""退出""置顶切换"。
- 窗口位置、尺寸、锁定状态持久化，下次启动恢复。

**窗口形态三档（设置可切）：**
| 档位 | 行为 | 实现成本 |
|---|---|---|
| **普通贴片（默认）** | 半透明、非置顶、正常 z-order | 低，原生支持 |
| 置顶贴片 | 始终浮在最上层（`always_on_top`） | 低，原生支持 |
| 嵌入壁纸层 | 画在桌面图标之下（Rainmeter 式） | 高，Win32 hack，列为 v2 可选 |

**验收：** 锁定后无法拖动；点托盘"隐藏贴片"后贴片消失、再点"显示"恢复；重启后位置/尺寸/锁定态一致；默认非置顶，其他窗口可遮挡它。

> 借鉴：小黄条 YYNote（托盘最小化仍保持桌面贴片可见）。

### R2. 可调节透明度，可自定义背景

**规格：**
- 透明度：滑块 10%–100%（含"全透明仅文字可见"档），实时预览。
- 背景：支持纯色、自定义图片（jpg/png/webp）、渐变三种模式；图片模式支持"拉伸/铺满/居中"。
- 预设：内置 4–6 套主题（深色/浅色/护眼绿/纸质感等）。
- 字体：可调字号、字体（提升可读性，配合透明背景）。

**验收：** 透明度滑块拖动时窗口实时变化；切换背景图立即生效；重启后保留。

> 借鉴：Rainmeter（全自定义）、小黄条（透明度含全透明）、敬业签（半透明窗口）。

### R3. 可自定义日程表显示时间范围（最小周，最大月）

**规格：**
- 显示范围档位：**周（7 天）/ 双周（14 天）/ 月（按自然月）**三档，状态栏一键切换或 Ctrl+滚轮缩放。
- 起始基准：周档以"本周一"或"今天"为起点（可配置）；月档以自然月 1 日为起点。
- 范围内渲染日期网格，超出范围可通过左右箭头翻页切换区间。

**验收：** 三档切换正确；翻页不丢失数据；范围选择持久化。

> 借鉴：TickTick（日/周/月切换）、Notion Calendar（缩放交互）。

### R4. 单击日程表某一格日期 → 展开当日日程

**规格：**
- 单击日期格 → **原地手风琴式展开**当日日程清单（向下/向侧展开，不弹新窗）。
- 再次单击或点空白处收起。
- 同时只允许一格展开（单展开模式）；切换展开格自动收起上一格。
- 展开区内可直接：勾选完成、新建、删除、编辑（对应 R5/R6）。
- 今日格默认高亮；空日期格显示"+添加"占位，降低录入摩擦。

**验收：** 单击展开/收起动画顺滑；切换格自动收起；展开区操作即时反映到网格。

> 借鉴：DesktopCal（双击日期格即编辑，本项目用单击）、Notion Calendar（格内展开动效）。

### R5. 完成态切换、删除线显示、完成鼓励语

**规格：**
- 切换方式（任选其一均触发）：① 单击日程条目左侧复选框；② 右键菜单"标记完成"；③ 选中条目按空格。
- 已完成：文字加**删除线 + 降饱和度/降透明度**，置底（仍可见，便于回看）。
- 鼓励语：标记为"完成"时（未完成→完成），从鼓励语库**随机抽一句**以轻量 Toast/气泡显示 2–3 秒，可配音效（默认关）。
- 鼓励语库：内置 30+ 句，用户可在设置中增删改；支持分类（励志/幽默/温柔）。
- 撤销：完成动作 5 秒内显示"撤销"小按钮（防误触）。

**验收：** 三种切换方式都生效；完成即时划线置底；每次完成弹出不同鼓励语；撤销可用。

> 借鉴：敬业签（自动划线）、Forest（自定义鼓励语库 + 音效）。

### R6. 添加日程：可选时间范围，自动填充范围内每一天

**规格：**
- 新建表单字段：标题、起止日期（日期选择器）、每日时段（可选，如"09:00"）、优先级、备注、是否有 ddl。
- **范围填充**：起止日期跨多天时，**为范围内每一天生成一条独立日程**（独立可勾选/删除，而非一条引用）。
- 快捷填充：自然语言识别"每天/工作日/周末"（可选 v2 增强）。
- 单日日程：起止同一天即普通日程。

**验收：** 选 7/1–7/5 添加"晨跑 07:00"，则 7/1~7/5 每天各生成一条；任一天独立完成不影响其他天。

> 借鉴：Todoist（自然语言重复）、TickTick（Duration 模式）。

### R7. 每天日程按时间排序；ddl 显示剩余天数并按紧迫程度着色

**规格：**
- 排序：展开区内日程默认按"时段升序"排列（无时段的排末尾，按创建时间）。
- ddl：日程可单独设置截止时间（日期或日期+时间）。
- 剩余天数显示：`还剩 N 天` / `今天` / `已超期 N 天`。
- **着色规则（按剩余天数自动）**：
  | 剩余时间 | 颜色 | 含义 |
  |---|---|---|
  | 已超期 | 深红 + 闪烁点 | 紧急 |
  | ≤ 1 天 | 红 | 今日内 |
  | ≤ 3 天 | 橙 | 较紧迫 |
  | ≤ 7 天 | 黄 | 注意 |
  | > 7 天 | 灰/默认 | 宽松 |
- 着色作用于日程条目的左侧色条/标签；用户可在设置中自定义阈值与颜色。

**验收：** 新增不同 ddl 的日程，颜色按表自动变化；跨过 0 点后颜色自动刷新。

> 借鉴：TickTick（Countdown + 按优先级着色）、Todoist（P1–P4 色阶）。

### R8. 开机自启动

**规格：**
- 设置中开关"开机自启"。
- 启动行为可配：① 启动后延迟 N 秒（避免抢占开机资源）；② 启动时恢复上次窗口位置/尺寸/锁定态；③ 启动后是否默认展开今日。
- 实现方式（按技术栈）：注册表 Run 键 / 计划任务 / 官方插件。

**验收：** 开启后重启系统，应用自动启动且位置恢复；关闭后不再自启。

> 借鉴：几乎所有成品标配；本项目额外加"延迟启动"避免打扰。

---

## 3. 技术选型

### 3.1 结论：**主选 Tauri 2.x（前端 + Rust），兜底 WPF/.NET 8**

| 维度 | Tauri 2.x（选定） | WPF/.NET 8（兜底） |
|---|---|---|
| 安装包体积 | 2.5–8 MB ✅ | 1–5 MB（框架依赖）/ 150 MB+（自包含） |
| 空闲内存 | 30–40 MB ✅ | 几十~100 MB |
| 透明无边框 | 原生（v2 透明仍 beta，需实测） | 原生，最稳 ✅ |
| 系统托盘 | 原生（v2 内置） | 第三方轻量库 |
| 开机自启 | 官方 plugin ✅ | 手写注册表 10 行 |
| 本地持久化 | tauri-plugin-sql (SQLite) | EF Core / SQLite |
| 单人开发效率 | 高（前端栈 + 少量 Rust） | 高（C# 单语言） |
| 跨平台 | ✅（未来可扩 macOS） | 仅 Windows |

**选定理由**：本项目核心是"轻量常驻 + 透明无边框托盘"，Tauri 在体积/内存上压倒性占优，且透明、无边框、托盘、自启、SQLite 全部有原生/官方插件覆盖，正好踩在需求清单上。前端用 Vue/React 单人开发高效。

**兜底触发条件**：若 Tauri v2 在 Windows 透明窗口 / 嵌入壁纸层 / 拖拽出现难以解决的 corner case，立即切 WPF——后者在这几点上是所有方案里最稳的。

**前端框架**：Vue 3 + TypeScript + Vite（生态成熟、单人维护友好；组件化契合日历网格）。

### 3.2 技术栈细节

- **Shell/后端**：Tauri 2.x（Rust）
- **前端**：Vue 3 + TypeScript + Vite + Pinia（状态管理）
- **UI 组件**：自绘日历网格（不依赖重型 UI 库，保证轻量与定制自由）；可按需引轻量组件（如日期选择器）
- **持久化**：SQLite（tauri-plugin-sql）；配置项用 JSON 文件
- **打包**：Tauri bundler → MSI/NSIS 安装包
- **依赖项（关键）**：
  - `tauri` 2.x 核心
  - `tauri-plugin-sql`（SQLite）
  - `tauri-plugin-autostart`（开机自启）
  - Tauri 内置 Tray + Window API（无边框/透明/拖拽/置顶）

---

## 4. 系统架构

### 4.1 分层

```
┌─────────────────────────────────────────────┐
│  前端层 (Vue 3 / TS)                          │
│  ├─ 日历网格组件 (CalendarGrid)               │
│  ├─ 日程展开面板 (DayPanel)                   │
│  ├─ 设置面板 (Settings)                       │
│  └─ Pinia Store (日程/配置状态)               │
├─────────────────────────────────────────────┤
│  Tauri Bridge (IPC: invoke / event)          │
├─────────────────────────────────────────────┤
│  Rust 后端 (Tauri Core)                       │
│  ├─ 窗口管理 (无边框/透明/拖拽/置顶/锁定)      │
│  ├─ 托盘管理 (Tray)                           │
│  ├─ 自启管理 (autostart plugin)               │
│  ├─ 数据访问层 (SQLite via tauri-plugin-sql)  │
│  └─ 系统调用 (开机延迟/位置恢复)              │
├─────────────────────────────────────────────┤
│  持久层 (SQLite: schedules.db + config.json)  │
└─────────────────────────────────────────────┘
```

### 4.2 关键流程

**启动流程：**
1. Tauri 启动 → 读 config.json 恢复窗口位置/尺寸/透明度/锁定态。
2. 创建无边框透明主窗口（桌面贴片）。
3. 创建托盘图标（不创建任务栏图标，或隐藏到托盘）。
4. 前端挂载 → 从 SQLite 加载当前时间范围日程 → 渲染网格。
5. 若开机自启且配置了延迟，先 sleep 再显示。

**完成日程流程（R5）：**
1. 用户点击复选框 → 前端 dispatch `toggleComplete(id)`。
2. Store 调 IPC → Rust 更新 SQLite `completed=true, completed_at=now`。
3. 前端更新 UI：划线 + 置底。
4. 前端从鼓励语库随机抽一句 → 显示 Toast 2–3s + 撤销按钮 5s。

**添加范围日程流程（R6）：**
1. 用户填表（标题、起止日期、时段…）→ 提交。
2. 前端按日期范围展开为 N 条记录数组。
3. 一次 IPC 批量插入 SQLite（事务）。
4. Store 刷新当前可见范围网格。

---

## 5. 数据模型

### 5.1 SQLite 表结构

```sql
-- 日程表（R6 范围填充 = 每天一条独立记录，用 group_id 关联）
CREATE TABLE schedules (
    id            INTEGER PRIMARY KEY AUTOINCREMENT,
    group_id      TEXT,            -- 同一次"范围添加"产生的记录共享同一 group_id
    title         TEXT NOT NULL,
    date          TEXT NOT NULL,   -- 'YYYY-MM-DD'，该日程归属的日期
    time_of_day   TEXT,            -- 'HH:MM'，可空（无时段排末尾）
    note          TEXT,
    priority      INTEGER DEFAULT 0,  -- 0 普通 / 1 重要 / 2 紧急（辅助排序与着色）
    has_ddl       INTEGER DEFAULT 0,
    ddl_at        TEXT,            -- 'YYYY-MM-DD HH:MM' 或 'YYYY-MM-DD'，可空
    completed     INTEGER DEFAULT 0,
    completed_at  TEXT,
    created_at    TEXT NOT NULL,
    updated_at    TEXT NOT NULL
);
CREATE INDEX idx_schedules_date ON schedules(date);
CREATE INDEX idx_schedules_group ON schedules(group_id);

-- 鼓励语库（R5）
CREATE TABLE encouragements (
    id       INTEGER PRIMARY KEY AUTOINCREMENT,
    text     TEXT NOT NULL,
    category TEXT,   -- '励志'/'幽默'/'温柔'
    enabled  INTEGER DEFAULT 1
);

-- 完成记录（可选，用于未来统计/成就）
CREATE TABLE completion_log (
    id           INTEGER PRIMARY KEY AUTOINCREMENT,
    schedule_id  INTEGER,
    completed_at TEXT
);
```

### 5.2 配置文件 config.json

```jsonc
{
  "window": {
    "x": 100, "y": 100, "width": 360, "height": 540,
    "locked": false,
    "opacity": 0.85,
    "always_on_top": false,        // 默认非置顶普通贴片；true 则置顶
    "background": { "mode": "image", "value": "bg/paper.jpg", "fit": "cover" }
  },
  "view": {
    "range": "week",              // week / biweek / month
    "week_start": "monday"
  },
  "startup": {
    "auto_start": true,
    "delay_seconds": 5,
    "expand_today_on_launch": true
  },
  "ddl_colors": {
    "overdue": "#c0392b",
    "le1":  "#e74c3c",
    "le3":  "#e67e22",
    "le7":  "#f1c40f",
    "gt7":  "#95a5a6"
  },
  "encouragement": {
    "sound": false,
    "undo_window_seconds": 5
  }
}
```

---

## 6. 交互与视觉规范

### 6.1 主界面布局（桌面贴片，默认 360×540）

```
┌──────────────────────────────────┐
│ 📅 桌面日程        🔒 锁定  ⚙ 设置 │  ← 顶部条（拖拽区，锁定后禁用拖拽）
├──────────────────────────────────┤
│  ◀  2026年7月  ▶   [周][双周][月] │  ← 范围切换
├──────────────────────────────────┤
│ 一  二  三  四  五  六  日        │
│ ─────────────────────────────    │
│  7   8   9  10  11  12  13       │  ← 日期网格
│ ●2  ●1      ●3            ← ddl 色点 + 当日条数
│ ─────────────────────────────    │
│ 14  15 [16] 17  18  19  20       │  ← 今日高亮
│ ─────────────────────────────    │
│  ┌─ 7月16日 今日 ▼ ────────────┐ │  ← 单击展开的当日面板（R4）
│  │ ☑ 晨跑 07:00       (还剩0天) │ │     完成项划线置底
│  │ ☐ 提交报告 ddl 18:00 🔴      │ │     ddl 色条 + 剩余天数
│  │ ☐ 看书 21:00                │ │
│  │ ＋ 添加日程…                │ │
│  └─────────────────────────────┘ │
└──────────────────────────────────┘
```

### 6.2 视觉规范要点

- **配色**：默认浅色纸质感；ddl 色阶独立于主题（见 5.2）。
- **字体**：系统默认无衬线 + 中文 fallback；可配。
- **圆角/阴影**：8–12px 圆角，轻阴影保证透明背景下可读。
- **动效**：展开/收起 200ms 缓动；完成时勾选有缩放反馈；鼓励语 Toast 淡入淡出。
- **可读性**：透明背景下文字加 text-shadow 或半透明底板，避免在花哨壁纸上糊掉。

---

## 7. 项目结构（初定）

```
桌面日程/
├── src-tauri/              # Rust 后端
│   ├── src/
│   │   ├── main.rs
│   │   ├── window.rs       # 无边框/透明/拖拽/锁定
│   │   ├── tray.rs         # 托盘
│   │   └── db.rs           # SQLite 初始化/迁移
│   ├── Cargo.toml
│   └── tauri.conf.json
├── src/                    # Vue 前端
│   ├── components/
│   │   ├── CalendarGrid.vue
│   │   ├── DayCell.vue
│   │   ├── DayPanel.vue    # 展开面板
│   │   ├── ScheduleItem.vue
│   │   └── SettingsPanel.vue
│   ├── stores/
│   │   ├── schedules.ts
│   │   └── config.ts
│   ├── api/                # 封装 Tauri IPC
│   ├── utils/              # ddl 着色/排序/日期范围展开
│   ├── App.vue
│   └── main.ts
├── 设计方案.md             # 本文档
├── 调研报告.md             # 调研详情
└── README.md
```

---

## 8. 里程碑（建议）

| 阶段 | 目标 | 产出 |
|---|---|---|
| **M0 技术验证** ✅ 已完成 | Tauri 透明无边框 + 拖拽 + 锁定 + 托盘 + 自启 demo | 一个空窗口满足 R1/R2/R8，**已确认无 corner case，技术栈选定 Tauri** |
| **M1 数据层** | SQLite 建表 + CRUD + 配置读写 | 后端 API 可用 |
| **M2 日历网格** | R3 范围切换 + 网格渲染 + 今日高亮 | 静态日程可见 |
| **M3 日程 CRUD** | R4 展开面板 + R6 范围填充 + R7 排序着色 | 核心可用 |
| **M4 完成态与鼓励** | R5 划线 + 鼓励语 + 撤销 | 体验闭环 |
| **M5 设置与外观** | R2 透明度/背景/字体 + 设置面板 | 可定制 |
| **M6 打包发布** | 便携 ZIP（+crt-static）+ 开机自启验证 | 可分发 v1.0 |

### M0 验证结果（2026-07-26 完成）

**结论：Tauri 2.x 技术栈选定，无需切 WPF 兜底。** 所有 R1/R2/R8 相关能力均已跑通。

实测验证项：
- ✅ 无边框透明窗口（`decorations:false, transparent:true`）正常渲染，桌面壁纸可见
- ✅ 非置顶普通窗口（`alwaysOnTop:false`）——其他窗口可正常遮挡，符合"普通贴片"定位
- ✅ 拖拽移动（前端 `startDragging()`）+ 锁定开关生效
- ✅ 透明度调节 + 背景色（前端 CSS 控制，见下方"关键技术修正"）
- ✅ 系统托盘（显示/隐藏/切换置顶/退出）+ 关闭按钮转隐藏到托盘
- ✅ 开机自启插件（`tauri-plugin-autostart`）已集成
- ✅ 静态 CRT 配置（`.cargo/config.toml` `+crt-static`）已就绪，为便携 ZIP 铺路
- ✅ 内存占用仅 **35MB**（符合 Tauri 轻量预期，远优于 Electron 的 200MB+）

**关键技术修正（实现中发现与设计的偏差）：**

1. **窗口透明度**：Tauri 2 **无窗口级 `set_alpha` API**（设计方案原假设有）。改由**前端 CSS `opacity`** 控制整个 widget 的视觉透明度——窗口本身 `transparent:true` 已开，CSS opacity 作用于内容层即可。这反而更简单且跨平台一致。
2. **托盘 API**：`menu_on_left_click` 在 Tauri 2.11 已废弃，改用 `show_menu_on_left_click`。
3. **权限配置**：`core:window:allow-set-alpha` 权限不存在（对应第 1 点）；`emit` 需要 `use tauri::Emitter`。
4. **开发环境**：本机网络对 crates.io 官方源不稳定，已配 `~/.cargo/config.toml` 走 rsproxy 镜像（sparse 协议），依赖下载正常。

> **关键决策点**：M0 已通过，**不切 WPF**。后续按 M1–M6 推进。

### M1–M5 实现进展（2026-07-27 完成）

M1 数据层、M2 日历网格、M3 日程 CRUD、M4 完成态与鼓励、M5 设置面板**全部完成并通过验证**。

**M1 数据层**
- `src-tauri/src/db.rs`：rusqlite（bundled）封装，建表/迁移 + 鼓励语种子（20 句，励志/幽默/温柔三类）。
- CRUD：`create_schedules`（按日期范围展开为多条独立记录，R6）、`toggle_completed`（切换+完成日志）、`update_schedule`（逐字段更新）、`delete_schedule`/`delete_group`。
- `src-tauri/src/config.rs`：AppConfig 序列化为 config.json，含 window/view/startup/ddl_colors/encouragement 五组，全字段 serde 默认值兜底。
- 关键修正：用 `with_db` 闭包辅助函数解决 `MutexGuard` 生命周期问题（不能在 `?` 表达式里解引用 guard）。

**M2 日历网格（R3）** — `CalendarGrid.vue`
- 周/双周/月三档切换；月视图自动补齐前置空白对齐星期列。
- 今日高亮；日期格内显示前 4 个日程的小圆点（已完成/未完成/ddl 着色）；待办计数角标。
- 月份导航 + "今天"按钮。

**M3 日程 CRUD（R4/R6/R7）** — `DayPanel.vue` + `ScheduleItem.vue` + `AddScheduleModal.vue`
- R4：单击日期格原地展开当日面板（手风琴式，单展开）。
- R6：添加表单支持单日/跨天，跨天自动填充范围内每一天；实时显示"将填充 N 天"。
- R7：展开面板按"未完成在前 + 时段升序 + 已完成置底"排序；ddl 按 5 级色阶着色（超期深红/≤1红/≤3橙/≤7黄/>7灰）并显示"还剩 N 天/今天/已超期 N 天"。

**M4 完成态与鼓励（R5）** — `ScheduleItem.vue` + `EncouragementToast.vue`
- 复选框单击切换；已完成项加删除线 + 降透明度 + 置底。
- 切换为"完成"时后端随机抽一句鼓励语，经 `encouragement` 事件推送到前端 Toast（顶部气泡，2.8s 自动消失，淡入淡出动画）。

**M5 设置面板（R2）** — `SettingsPanel.vue`
- 透明度滑块（20%–100%）、背景颜色选择器，改动即时持久化到 config.json。
- 置顶开关、开机自启开关（联动 autostart 插件）。

**验证结果**
- ✅ `cargo check` 通过（仅 3 个无害 dead_code warning）。
- ✅ `vue-tsc --noEmit` 类型检查通过（退出码 0）。
- ✅ `tauri dev` 启动成功，内存约 36 MB。
- ✅ 数据链路测试：直接向 SQLite 插入 5 类测试日程（单日/跨天/今日ddl/3天后ddl/已完成），重启应用后前端正确渲染——网格圆点+计数、展开面板按时间排序、已完成置底、ddl 着色逻辑均符合预期。测试后已清理数据。

---

## 9. 风险与待决问题

| 风险/问题 | 说明 | 应对 |
|---|---|---|
| Tauri v2 透明窗口仍 beta | Windows 上可能有拖拽/穿透异常 | M0 优先验证；不行切 WPF |
| "嵌入壁纸层"实现复杂 | 需调 Win32 API 把窗口画到桌面图标层之下 | 作为 v1 可选；v1 先做"置顶半透明" |
| WebView2 依赖 | 用户机器需有 WebView2 运行时 | 安装包内置 bootstrapper |
| 范围填充产生大量记录 | 一年每天一条 = 365 行 | 可接受；按需懒加载可见范围 |
| 鼓励语疲劳 | 每次同一句会腻 | 随机抽取 + 分类 + 用户可编辑 |
| 多显示器/分辨率变化 | 窗口位置可能跑出屏幕 | 启动时校验位置在可视区内，否则归位 |

---

## 10. 下一步行动

1. **立即**：执行 M0——用 Tauri 2.x 搭一个透明无边框 + 托盘 + 自启的最小 demo，验证 R1/R2/R8 的技术可行性（**这是决定技术栈去留的 gate**）。
2. M0 通过后，按里程碑 M1–M6 推进。
3. M0 不通过 → 切 WPF/.NET 8，重做同等的透明无边框托盘 demo。

---

## 附录 A：调研结论速查

详见 `调研报告.md`。核心借鉴映射：

| 需求 | 借鉴来源 |
|---|---|
| R1 嵌入/锁定/托盘可见 | 小黄条、DesktopCal |
| R2 透明/背景 | Rainmeter、小黄条、敬业签 |
| R3 时间范围 | TickTick、Notion Calendar |
| R4 日期格展开 | DesktopCal（单击改进）、Notion Calendar |
| R5 完成态/鼓励 | 敬业签（划线）+ Forest（鼓励语） |
| R6 范围填充 | Todoist（自然语言）、TickTick（Duration） |
| R7 排序+ddl 着色 | TickTick（Countdown+着色）、Todoist（P1–P4） |
| R8 开机自启 | 标配 |

技术选型详见调研报告"推荐排序"：**Tauri 2.x 首选，WPF/.NET 8 兜底**。
