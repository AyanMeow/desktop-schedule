# 技术探究方案

> 日期：2026-07-27
> 性质：可行性研究与方案设计，**尚未实现**，待你确认后再开发

本文档针对你提出的三个探究点，给出技术路线、可行性评估、实现方案和工作量估算。

---

## 探究点 1：托盘化后仍保持桌面贴片显示

### 需求理解
应用"关闭任务栏窗口/最小化到托盘"后，**桌面贴片依然可见**，而不是随窗口一起消失。即：托盘只控制"后台进程常驻 + 任务栏图标"，贴片是独立常驻的。

### 当前实现现状
目前是**单窗口**架构：托盘菜单的"显示/隐藏"调用 `win.hide()/show()`，隐藏后贴片也消失。关闭按钮拦截后转 `hide()`，同样消失。这与你的诉求有差距。

### 可行性：✅ 完全可行

### 技术方案

**方案 A：双窗口分离（推荐）**
- 拆成两个窗口：
  - `main` 主窗口（贴片，常驻可见，`skip_taskbar: true` 不占任务栏）
  - `panel` 控制台窗口（可选的设置/管理窗口，走正常任务栏）
- "最小化到托盘" = 只隐藏 `panel`，`main` 贴片始终 `show()`
- 托盘菜单"显示/隐藏"切换的是 `panel`，而非 `main`

**改动点：**
1. `tauri.conf.json` 加第二个窗口配置（`skip_taskbar: true` 的贴片窗口）
2. `lib.rs` 的 `on_window_event` 区分窗口 label：`main` 关闭=隐藏贴片（或退出），`panel` 关闭=只隐藏 panel
3. 托盘菜单"显示/隐藏"逻辑改为操作 `panel`
4. 贴片窗口设 `skipTaskbar: true`，它本来就不在任务栏，"托盘化"对它无意义

**方案 B：贴片窗口直接 skip_taskbar + 永不隐藏**
- 贴片窗口 `skipTaskbar: true`（不在任务栏出现），且代码里**永远不调用 hide()**
- "托盘"只作为进程入口（退出入口在托盘菜单）
- 关闭按钮直接禁用或改为"最小化到内存"（什么都不做）
- 这是最简方案，但失去"临时隐藏贴片"的能力

**推荐**：方案 B 起步（最简，2 小时），未来需要管理界面再加方案 A 的第二窗口。

### 工作量：2-4 小时

### 风险
- Win11 的 `skip_taskbar` 在某些版本表现不一致（任务栏仍有残影），需实测
- 双窗口架构会增加状态同步复杂度（配置变更要广播到两个窗口）

---

## 探究点 2：低透明度（~40%）下保持文字清晰

### 需求理解
背景调到 40% 透明时，文字也跟着变虚，难以辨认。希望**只让背景半透明，文字始终清晰**。

### 当前实现现状
已经做了背景层分离：`.bg-layer`（透明度作用于此）+ `.fg`（不透明前景）。理论上文字应该清晰。**如果仍模糊，原因可能是：**
1. 背景层透明度高时，壁纸花哨，文字对比度不足（并非文字本身透明）
2. `.fg` 层没有自己的底色，纯靠 currentColor 在花背景上不够清晰

### 可行性：✅ 完全可行（当前架构已支持，需补强）

### 技术方案

**核心思路：给前景文字加"可读性增强"层，而非依赖背景透明度。**

**方案 1：文字描边/阴影（最简单，立即见效）**
```css
.fg {
  text-shadow: 0 1px 3px rgba(0,0,0,0.8);  /* 深色文字配浅阴影 */
  /* 或浅色文字：text-shadow: 0 1px 3px rgba(0,0,0,0.6); */
}
```
- 优点：1 行 CSS，所有文字立即清晰
- 缺点：阴影会稍微"糊"，精致感略降

**方案 2：前景半透明底板（推荐，效果最稳）**
给 `.fg` 加一个半透明背景层（独立的第三层），与 bg-layer 解耦：
```
.widget
  ├── .bg-layer       (opacity: 用户设置，如 40%)  ← 壁纸/底色
  ├── .fg-veil        (opacity: 固定，如浅色 60% 白或深色 60% 黑) ← 文字底板
  └── .fg             (不透明) ← 文字图标
```
- `.fg-veil` 是一层"磨砂玻璃"，保证文字背后永远有一层柔和底色
- 用户调的透明度只影响最底层 bg-layer，veil 始终给文字托底

**方案 3：动态对比度（高级）**
根据壁纸/背景色亮度自动选择文字黑/白，并自动调 veil 透明度。需要采样背景色（复杂，性价比低）。

**推荐**：方案 1（描边）立即加，5 分钟；方案 2（veil）作为正式解，30 分钟。

### 工作量：30 分钟（方案 2）

### 验证
- 把透明度调到 30%，文字应仍清晰可读
- 切换深色/浅色/图片背景，文字对比度都足够

---

## 探究点 3：加入天气显示

### 需求理解
在贴片上显示当前天气（温度、天气状况、城市）。

### 可行性：✅ 可行，但需选择数据源

### 数据源对比

| 数据源 | 免费 | 免 Key | 稳定性 | 国内城市 | 推荐度 |
|---|---|---|---|---|---|
| **和风天气 (QWeather)** | ✅ 每日 1000 次 | 需注册 Key | 高 | ✅ 极全 | ⭐⭐⭐⭐⭐ |
| Open-Meteo | ✅ 完全免费 | **无需 Key** | 高 | ✅ | ⭐⭐⭐⭐ |
| 心知天气 (Seniverse) | ✅ 有限免费 | 需 Key | 高 | ✅ | ⭐⭐⭐ |
| 高德/百度天气 | 部分免费 | 需 Key | 高 | ✅ | ⭐⭐⭐ |

**推荐 Open-Meteo**（完全免费、无需注册 Key、全球覆盖、API 简洁）：
```
https://api.open-meteo.com/v1/forecast?latitude=39.9&longitude=116.4&current=temperature_2m,weather_code
```
返回 JSON，`current.temperature_2m` + `current.weather_code`（WMO 编码，可映射为中文天气+图标）。

### 技术方案

**架构：Rust 端定时拉取，前端展示**

1. **定位**：
   - 简单版：用户在设置里手选城市（提供常用城市下拉）
   - 进阶版：用 IP 定位（`ip-api.com` 免费）或系统定位（需 `tauri-plugin-geolocation`）
   - 推荐：手选城市起步（无需定位权限，离线可控）

2. **拉取**（Rust 端）：
   - 加 `reqwest` 依赖（HTTP 客户端）
   - 写 `fetch_weather(lat, lon)` 命令，调 Open-Meteo
   - 每 30 分钟自动刷新一次（用 `tokio::spawn` + `interval`）

3. **展示**（前端）：
   - 顶栏右侧加天气小组件：图标 + 温度 + 城市
   - WMO weather_code 映射为 SVG 图标（晴/云/雨/雪，复用 Icon 组件加几个新图标）+ 中文描述

4. **配置**：
   - config 加 `weather: { enabled, city, lat, lon }`
   - 设置面板加"天气"分组：开关 + 城市选择

### 改动点
- `Cargo.toml` 加 `reqwest = { version = "0.12", features = ["json"] }`
- `db/config` 加 weather 配置
- `lib.rs` 加 `fetch_weather` 命令 + 定时任务
- 新建 `WeatherBadge.vue` 组件
- Icon.vue 加天气图标（sun/cloud/rain/snow）
- 设置面板加天气分组

### 工作量：3-4 小时

### 风险
- **网络依赖**：无网时天气区显示"–"或上次缓存值
- **国内访问 Open-Meteo**：实测可达，但偶发慢；可加超时 fallback 到缓存
- 隐私：定位需用户授权；手选城市无此问题

---

## 三个探究点的优先级建议

| 探究点 | 价值 | 难度 | 建议优先级 |
|---|---|---|---|
| **2. 低透明度文字清晰** | 高（直接影响可用性） | 低（30 分钟） | 🔴 先做 |
| **1. 托盘化仍显示** | 中（核心体验相关） | 中（2-4 小时） | 🟡 其次 |
| **3. 天气显示** | 中低（锦上添花） | 中（3-4 小时） | 🟢 最后 |

确认要实现哪些、用什么方案，我再动手。方案文档不改代码，等你拍板。
