# 桌面日程 · 快速开始（接手指南）

> 面向接手本项目的智能体/开发者。阅读本文 + `设计方案.md` 即可全面掌握项目。

---

## 1. 项目是什么

**桌面日程**——一款 Windows 桌面常驻的半透明日程管理小组件。形态是浮在桌面上的无边框贴片（非置顶、可被遮挡），用于快速查看/管理日程，配合独立的任务栏控制面板与系统托盘。

核心一句话：**它是桌面的一部分，而非一个应用。**

---

## 2. 技术栈

| 层 | 技术 | 版本要点 |
|---|---|---|
| 桌面框架 | **Tauri 2.x**（Rust + 系统 WebView2） | 无边框透明窗口、托盘、双窗口 |
| 后端语言 | **Rust**（stable-msvc 工具链） | 编译为单文件 exe |
| 前端框架 | **Vue 3**（`<script setup>` + Composition API） | TypeScript |
| 状态管理 | **Pinia 2** | schedules / config / weather 三 store |
| 构建 | **Vite 6** + vue-tsc | `npm run tauri dev/build` |
| 数据库 | **SQLite**（rusqlite 0.32，bundled 自带 C 源） | 本地单文件 `schedules.db` |
| HTTP | **reqwest 0.12**（rustls-tls） | 拉天气 |
| 配置 | serde_json → `config.json` | 窗口几何/外观/视图/自启/天气/ddl配色 |

**不使用**：tauri-plugin-sql（直接 rusqlite 更可控）、tauri-plugin-autostart（实测报 os error 2，已自实现 winreg 注册表写入）。

---

## 3. 环境配置（Windows 必需）

```
1. Microsoft C++ Build Tools 2022   ← 勾选 "Desktop development with C++"
2. Rust (rustup, stable-msvc)       ← rustup default stable-msvc
3. Node.js LTS
4. WebView2 运行时                   ← Win10/11 一般已预装
```

国内网络加速（已完成）：
- `~/.cargo/config.toml` 配 rsproxy 镜像（crates.io sparse 协议）
- 静态 CRT：`src-tauri/.cargo/config.toml` 设 `target-feature=+crt-static`（便携 exe 不依赖 vcruntime）

**启动**：双击 `启动测试.bat` 选 1，或：
```bash
cd desktop-schedule
npm run tauri dev      # 开发，热重载，首次编译 2-3 分钟
```

---

## 4. 目录结构

```
E:\日程管理\
├── 设计方案.md              # 产品设计、需求映射、架构、里程碑、实现进展
├── 调研报告.md              # 竞品与技术栈调研
├── 技术探究方案.md          # 探究1/2/3 的方案文档
├── 快速开始.md              # ← 本文件
├── 启动测试.bat             # 构建/检查/清理的菜单式启动器
├── 测试日程数据.txt         # 11 条覆盖各场景的导入测试数据
└── desktop-schedule/        # 项目根
    ├── src/                 # Vue 前端
    │   ├── App.vue          # 主界面（贴片+控制面板双模式，按 #panel hash 区分）
    │   ├── themes.ts        # 6 套配色预设 + ddl 色阶推导
    │   ├── types.ts         # TS 类型（与 Rust serde 结构对应）
    │   ├── api.ts           # Tauri IPC 调用封装
    │   ├── utils/date.ts    # 日期范围计算 + ddlStatus 着色
    │   ├── stores/          # Pinia: schedules / config / weather
    │   └── components/      # 9 个组件（见下）
    └── src-tauri/
        ├── src/
        │   ├── lib.rs       # 主入口：命令注册 + 托盘 + 双窗口事件
        │   ├── db.rs        # SQLite CRUD + 导入导出 + 鼓励语
        │   ├── config.rs    # AppConfig 序列化与默认值
        │   ├── weather.rs   # Open-Meteo 拉取 + 定时刷新
        │   └── window_state.rs  # 窗口锁定状态
        ├── capabilities/default.json  # 前端可调用的权限白名单（关键！）
        ├── tauri.conf.json  # 双窗口配置（main 贴片 + taskbar 面板）
        └── .cargo/config.toml  # +crt-static
```

### 组件职责

| 组件 | 职责 |
|---|---|
| `App.vue` | 根容器：背景层/磨砂底板/前景三层；顶栏拖拽+resize；按 `#panel` 渲染贴片或控制面板；窗口几何持久化 |
| `CalendarGrid.vue` | 日历网格：周/双周/月切换；日期格显示日程标题+天气 |
| `DayPanel.vue` | 单击日期格展开的当日面板 |
| `ScheduleItem.vue` | 单条日程：完成切换/删除线/ddl着色/附件打开/多天删除提示 |
| `AddScheduleModal.vue` | 添加表单：日期选择+范围填充+自动ddl+附件 |
| `SettingsPanel.vue` | 设置：主题/配色/字体/透明度/背景/天气/窗口/导入导出 |
| `DatePicker.vue` | 日历式日期选择器（替换原生 input[type=date]） |
| `WeatherBadge.vue` | 顶栏天气徽章 |
| `EncouragementToast.vue` | 完成日程时的随机鼓励语气泡 |
| `Icon.vue` | SVG 线条图标库（Lucide 风格，currentColor） |

---

## 5. 重要功能与实现要点

### 双窗口架构（核心，易踩坑）
- **main 窗口**：贴片，`skipTaskbar:true`，无边框透明，**永不隐藏**，常驻桌面
- **taskbar 窗口**：控制面板，普通装饰窗口，占任务栏，`url: "index.html#panel"`
- 前端用 `window.location.hash === '#panel'` 区分渲染哪个界面
- `lib.rs` 的 `on_window_event` 按 `window.label()` 分流：main 拦截关闭，taskbar 关闭=隐藏
- **改窗口行为必须同时考虑两个窗口**

### 持久化三件套（近期重点修复）
| 项 | 存储位置 | 机制 |
|---|---|---|
| 日程数据 | `%APPDATA%\com.zcode.schedule\schedules.db` | rusqlite CRUD |
| 应用配置 | 同目录 `config.json` | serde_json，全字段带 `#[serde(default)]` 兜底 |
| 开机自启 | 注册表 `HKCU\...\Run\DesktopSchedule` | winreg 自实现（**非** autostart 插件） |

配置/数据库路径：`C:\Users\<用户>\AppData\Roaming\com.zcode.schedule\`

### 配色系统（CSS 变量驱动）
- `themes.ts` 定义 6 套 Palette，每套含深/浅双模式
- `App.vue` 的 `rootStyle` 根据 `theme_name` + `bg_mode` 注入 CSS 变量：`--accent`/`--warning`/`--danger`/`--ddl-*`/`--veil-bg`/`--modal-bg`
- **所有颜色必须用 CSS 变量，禁止硬编码**（曾有 #6c8cff 散落各处导致切主题不生效的问题）
- 三层视觉结构：`.bg-layer`（用户透明度）→ `.fg-veil`（磨砂底板，固定托底保文字清晰）→ `.fg`（不透明前景）

### ddl 着色
- `utils/date.ts` 的 `ddlStatus()` 计算剩余天数返回 `level`（overdue/le1/le3/le7/gt7）
- 颜色由 CSS 类 `.ddl-lv-*` → `var(--ddl-*)` 驱动，**不传 colors 参数**（已改为可选）

### 天气
- `weather.rs` 调 Open-Meteo（免费无 Key），拉 current + 7 天 daily
- 后台 `tokio` 每 30 分钟刷新，经 `weather-updated` 事件广播
- 全局共享：`stores/weather.ts`，日历格用 `dailyByDate(dateISO)` 取某日预报

### 自定义命令清单（lib.rs invoke_handler）
日程：`list_schedules` `create_schedule` `toggle_complete` `update_schedule` `delete_schedule` `delete_group` `count_group` `open_attachment`
导入导出：`export_schedules` `import_schedules`
天气：`fetch_weather` `get_last_weather`
配置：`get_config` `save_config`
窗口：`toggle_lock` `set_opacity` `toggle_always_on_top` `set_autostart` `is_autostart_enabled`
鼓励语：`list_encouragements`

---

## 6. 开发要求（重要约定）

1. **新增 Tauri 命令**：在 `lib.rs` 写 `#[tauri::command]`，并加到 `invoke_handler![]` 宏，**还要在 `capabilities/default.json` 加权限**（若用到 core:* 资源）。三者缺一，前端调用静默失败（无报错）。
2. **前端调 IPC**：走 `src/api.ts` 封装，类型来自 `types.ts`（与 Rust 结构字段名一致）。
3. **颜色**：只用 CSS 变量；新增主题色板加到 `themes.ts` 的 `PALETTES`。
4. **加配置项**：`config.rs` 结构体加字段 + `#[serde(default = "fn")]` + Default impl；同步 `types.ts` + `stores/config.ts` 默认值。**已有 config.json 不会自动获得新字段**——靠 serde default 兜底，无需迁移。
5. **DB schema 改动**：在 `db.rs` 的 `migrate()` 加 `ALTER TABLE` 兼容旧库（参考 attachment 字段的迁移写法）。
6. **窗口 API 权限**：setPosition/setSize/outerPosition 等走 IPC 的窗口 API 需在 capabilities 加 `core:window:allow-*` 权限。
7. **checkbox 双向**：用 `:checked` + `@change` 时，必须从 `(e.target as HTMLInputElement).checked` 取值，不能依赖 ref 旧值（曾导致自启开关失效）。
8. **提交规范**：每个功能/修复独立 commit，中文消息，`feat/fix/style/refactor` 前缀。

---

## 7. 常用命令

```bash
npm run tauri dev              # 开发
npm run tauri build -- --no-bundle   # 便携 exe（→ target/release/desktop-schedule.exe）
npm run tauri build            # 安装包（MSI/NSIS）
cd src-tauri && cargo check    # 仅检查 Rust
npx vue-tsc --noEmit           # 仅检查前端类型
```

构建产物位置：`desktop-schedule/src-tauri/target/release/desktop-schedule.exe`（单文件可移植，已静态链接 CRT）。

---

## 8. 已知约束与待办

- **打包**：便携 exe 依赖系统 WebView2（Win11 默认有）；纯离线环境需额外捆绑。
- **测试数据**：`测试日程数据.txt` 含 11 条覆盖各场景的日程，导入即用。
- **未做**：窗口位置的多显示器边界校验、撤销完成按钮、自启延迟、键盘快捷键、自定义应用图标。
- **3 个无害 warning**：`is_locked` 等 dead_code，后续接入即可消除。

---

## 9. 版本进度（截至 2026-07-29）

| 里程碑 | 状态 |
|---|---|
| M0 技术验证 | ✅ Tauri 透明无边框 + 托盘 + 自启 |
| M1 数据层 | ✅ SQLite + 配置持久化 |
| M2 日历网格 | ✅ 周/双周/月 + 今日高亮 |
| M3 日程 CRUD | ✅ 展开/范围填充/排序/ddl着色 |
| M4 完成态与鼓励 | ✅ 划线 + 随机鼓励语 |
| M5 设置与外观 | ✅ 透明度/背景/字体/配色 |
| 探究1 双窗口 | ✅ 托盘化仍显示贴片 |
| 探究2 磨砂底板 | ✅ 低透明度文字清晰 |
| 探究3 天气 | ✅ 当前+7天预报 |
| 增强 | ✅ 配色系统/日历格标题/导入导出/SVG图标/双窗口/天气/窗口几何持久化/开机自启 |
| M6 打包发布 | ⏳ 可随时 `tauri build --no-bundle` |

8 项原始需求（R1-R8）全部覆盖。详细演进见 git log（约 16 个 commit）。
